# Database
DATABASE_URI=postgresql://payload:payload@localhost:5432/payload

# Payload CMS
PAYLOAD_SECRET=        # openssl rand -hex 32

# Public URL (used for preview links in Payload admin)
SERVER_URL=https://cv.mfrh.xyz
