# PostgreSQL
POSTGRES_USER=payload
POSTGRES_PASSWORD=     # openssl rand -hex 16
POSTGRES_DB=payload

# Payload CMS
PAYLOAD_SECRET=        # openssl rand -hex 32

# Port exposed on host (Caddy reverse-proxies to this)
CV_PORT=3100

# Public URL (used for preview links in Payload admin)
NEXT_PUBLIC_SERVER_URL=https://cv.mfrh.xyz
